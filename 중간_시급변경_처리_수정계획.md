# 중간 시급 변경 처리 수정 계획서

## 1. 개요

### 1.1 문제 정의
직원이 한 달 중간에 진급 또는 계약 변경으로 인해 시급이 변경되는 경우, 현재 HTML 급여명세서에서는 **마지막 시급만 표시**되어 산출식과 실제 계산 금액이 불일치하는 문제가 발생한다.

### 1.2 발견 사례 (12월 급여포함.xlsx 분석 결과)
| 직원명 | 사번 | 시급 변경 내역 | 해당 월 |
|--------|------|----------------|---------|
| 오수민 | 190335407 | 10,030원 → 13,000원 | 10월 |
| 최재희 | 190335415 | 10,030원 → 13,000원 | 10월 |
| 이서연 | 190335416 | 10,030원 → 13,000원 | 10월 |
| 김나연 | 190335420 | 10,030원 → 11,000원 | 10월 |
| 홍유민 | 190335406 | 10,120원 → 11,000원 | 11~12월 |
| 오수민 | 190335407 | 10,030원 → 11,000원 | 12월 |

### 1.3 현재 문제 예시
```
[입력 데이터]
- 1일: 시급 10,000원 × 8시간 = 80,000원
- 2일: 시급 10,000원 × 8시간 = 80,000원
- 15일: 시급 11,000원 × 8시간 = 88,000원 (진급)
- 16일: 시급 11,000원 × 8시간 = 88,000원

[현재 HTML 결과]
기본급: 336,000원 (✅ 정확)
시급: 11,000원 (⚠️ 마지막 시급만 표시)
산출식: 32시간 × 11,000원 = 352,000원 (❌ 오류 - 실제와 16,000원 차이)
```

---

## 2. 고용노동부 법적 기준

### 2.1 근로기준법 제6조 (임금의 정의)
> 임금은 **근로의 대가**로 지급되는 것

- 시급 변경 시 변경일 기준으로 각각 계산해야 함
- 현재 시스템의 기본급 계산은 법적으로 정확함

### 2.2 최저임금법
> 동일 근로자에게 **최저임금 이상** 지급 의무

- 각 근무일의 시급이 최저임금 이상이어야 함
- 시급 변경 시에도 각 기간별 최저임금 준수 필요

### 2.3 주휴수당 계산 (고용노동부 해석)
> "주휴수당은 **해당 주의 시간급 임금**을 기준으로 계산"

- 주휴수당도 주별로 다른 시급 적용 가능

### 2.4 결론
| 항목 | 현재 상태 | 법적 적합성 |
|------|----------|------------|
| **기본급 계산** | ✅ 각 시급×시간 합산 | 정확 |
| **연장수당 계산** | ✅ 해당일 시급 기준 | 정확 |
| **주휴수당 계산** | ✅ 해당 주의 시급 기준 | 정확 |
| **명세서 표시** | ⚠️ 마지막 시급만 표시 | 개선 필요 |
| **산출식 표시** | ❌ 마지막 시급 기준 | 개선 필요 |

---

## 3. 수정 방안

### 3.1 검토된 옵션

#### 옵션 1: 가중평균 시급 표시 (권장)
```
기본급: 336,000원
시급: 10,500원 (가중평균)
산출식: 32시간 × 10,500원 = 336,000원
```
**장점**: 
- 실제 계산과 일치
- 직관적이고 이해하기 쉬움
- 고용노동부 지침 위반 없음
- 대부분의 급여시스템에서 채택하는 방식

#### 옵션 2: 시급 이력 상세 표시
```
기본급: 336,000원
├ 1~2일: 10,000원 × 16시간 = 160,000원
└ 15~16일: 11,000원 × 16시간 = 176,000원
```
**장점**: 투명성 최고
**단점**: 명세서 복잡해짐, 모든 항목에 적용 시 가독성 저하

#### 옵션 3: 현행 유지
```
기본급: 336,000원 (계산은 정확)
시급: 11,000원 (최종 시급만 표시)
```
**단점**: 산출식과 실제 계산이 다름, 직원 오해 가능

### 3.2 최종 선택: 옵션 1 (가중평균 시급 표시)

**선택 이유**:
1. 고용노동부에서도 권장하는 방식
2. 산출식과 실제 계산의 일치성 확보
3. 명세서의 간결성 유지
4. 대부분의 ERP/급여시스템 표준 방식

---

## 4. 상세 수정 계획

### 4.1 Phase 1: logic.py 수정

#### 4.1.1 수정 위치
- 함수: `_prepare_payslip_data`
- 파일: `/Volumes/MyExternal/급여명세서/logic.py`

#### 4.1.2 수정 내용

**A. 가중평균 시급 계산 로직 추가**
```python
# 기존 코드
hourly_rate = int(user_summary.get('hourly_rate', 0))

# 수정 코드 - 가중평균 시급 계산
base_pay_amount = user_summary.get('base_pay', 0)
work_minutes = user_summary.get('근무시간_분', 0)
work_hours = work_minutes / 60.0 if work_minutes > 0 else 0

# 가중평균 시급 계산 (기본급 ÷ 총근무시간)
if work_hours > 0:
    weighted_hourly_rate = round(base_pay_amount / work_hours)
else:
    weighted_hourly_rate = int(user_summary.get('hourly_rate', 0))

# 시급 변경 여부 확인
last_rate = int(user_summary.get('hourly_rate', 0))
rate_changed = abs(weighted_hourly_rate - last_rate) > 1  # 1원 이상 차이
```

**B. 산출식 수정**
```python
# 기존
if explanation_options.get('base_pay_explanation', True):
    calculation_note_1 = f"근무시간 {total_hours:.1f}시간 × 시급 {hourly_rate:,}원 = {base_pay_amount:,.0f}원"

# 수정
if explanation_options.get('base_pay_explanation', True):
    if rate_changed:
        # 시급 변경된 경우 가중평균 표시
        calculation_note_1 = f"근무시간 {total_hours:.1f}시간 × 시급 {weighted_hourly_rate:,}원 (가중평균) = {base_pay_amount:,.0f}원"
    else:
        # 시급 고정인 경우 기존 방식 유지
        calculation_note_1 = f"근무시간 {total_hours:.1f}시간 × 시급 {hourly_rate:,}원 = {base_pay_amount:,.0f}원"
```

**C. 반환 데이터에 가중평균 시급 추가**
```python
return {
    # ... 기존 데이터 ...
    'hourly_rate': hourly_rate,                    # 원래 시급 (마지막 시급)
    'weighted_hourly_rate': weighted_hourly_rate,   # 가중평균 시급 (신규)
    'rate_changed': rate_changed,                   # 시급 변경 여부 (신규)
    # ...
}
```

### 4.2 Phase 2: payslip_template.html 수정

#### 4.2.1 수정 위치
- 파일: `/Volumes/MyExternal/급여명세서/payslip_template.html`

#### 4.2.2 수정 내용

**A. 시급 표시 부분 수정**
```html
<!-- 기존 -->
<p><strong>시급:</strong> {payslip_data['hourly_rate']:,.0f}원</p>

<!-- 수정 -->
<p><strong>시급:</strong> 
    {payslip_data['weighted_hourly_rate']:,.0f}원
    {% if payslip_data['rate_changed'] %}
        <span style="font-size: 0.9em; color: #666;">(가중평균)</span>
    {% endif %}
</p>
```

**B. 기본급 산출식 수정 (선택적)**
```html
<!-- 기존 -->
{% if show_base_pay_note %}<p><strong>기본급 산출식:</strong> {{ calculation_note_1 }}</p>{% endif %}

<!-- 수정 - calculation_note_1에 이미 가중평균 반영됨 -->
{% if show_base_pay_note %}<p><strong>기본급 산출식:</strong> {{ calculation_note_1 }}</p>{% endif %}
```

### 4.3 Phase 3: 테스트 및 검증

#### 4.3.1 테스트 시나리오

**시나리오 1: 시급 변경 직원 (오수민)**
- 입력: 10,030원 → 13,000원 (10월)
- 예상 결과:
  - 기본급: 실제 계산값
  - 시급: 가중평균 값 표시
  - 산출식: 가중평균 시급 × 시간 = 기본급 (일치)

**시나리오 2: 시급 고정 직원 (홍유민 - 10월 제외)**
- 입력: 10,030원 고정
- 예상 결과:
  - 기존과 동일하게 표시
  - "(가중평균)" 문구 미표시

**시나리오 3: 최저임금 근접 직원**
- 입력: 최저임금(10,030원) 근처에서 변경
- 예상 결과:
  - 최저임금 위반 여부 정상 체크
  - 가중평균이 최저임금 이상인지 확인

#### 4.3.2 검증 항목
| 검증 항목 | 방법 | 기대 결과 |
|----------|------|----------|
| 산출식 = 실제 계산 | 수동 계산 비교 | 완전 일치 |
| 가중평균 정확성 | (기본급 ÷ 시간) 계산 | 소수점 반올림 일치 |
| 시급 변경 감지 | 1원 이상 차이 | 정확히 감지 |
| HTML 렌더링 | 브라우저 확인 | 정상 표시 |
| 엑셀 출력 | 엑셀 파일 확인 | 정상 출력 |

---

## 5. 구현 일정

| 단계 | 작업 내용 | 소요 시간 | 담당 |
|------|----------|----------|------|
| 1일차 | logic.py 수정 (가중평균 계산) | 2시간 | 개발자 |
| 1일차 | payslip_template.html 수정 | 1시간 | 개발자 |
| 2일차 | 테스트 및 디버깅 | 2시간 | 개발자 |
| 2일차 | 실 데이터 검증 (12월 파일) | 1시간 | 검증자 |
| 3일차 | 배포 및 문서화 | 1시간 | 개발자 |

**총 예상 소요 시간: 7시간 (1.5일)**

---

## 6. 리스크 및 대응

| 리스크 | 발생 가능성 | 영향도 | 대응 방안 |
|--------|-----------|--------|----------|
| 가중평균 계산 오차 | 중 | 중 | 소수점 처리 로직 검증 |
| 소수점 반올림 차이 | 중 | 낮 | round() 함수 사용 |
| 기존 데이터 호환성 | 낮 | 중 | 기존 시급 필드 유지 |
| 성능 저하 | 낮 | 낮 | 단순 계산이므로 미미 |

---

## 7. 참고 자료

### 7.1 관련 파일
- `/Volumes/MyExternal/급여명세서/logic.py` - 핵심 계산 로직
- `/Volumes/MyExternal/급여명세서/payslip_template.html` - HTML 템플릿
- `/Volumes/MyExternal/급여명세서/12월 급여포함.xlsx` - 테스트 데이터

### 7.2 관련 함수
- `calculate_salary()` - 급여 계산
- `create_user_summaries()` - 직원별 요약
- `_prepare_payslip_data()` - 명세서 데이터 준비 (수정 대상)
- `generate_payslips_html()` - HTML 생성

---

## 8. 승인

| 역할 | 이름 | 서명 | 날짜 |
|------|------|------|------|
| 작성자 | | | |
| 검토자 | | | |
| 승인자 | | | |

---

*문서 버전: 1.0*  
*작성일: 2026-02-11*  
*최종 수정일: 2026-02-11*
