# 주휴수당 HTML 출력 문제 해결 계획서

> **작성일**: 2026년 2월 9일  
> **문제**: 2025년 12월 급여명세서 생성 시 HTML 주석에 9월/10월 주휴수당 정보 잘못 표시  
> **해결 방식**: 근본적 해결 (모든 년도/월에 자동 적용)

---

## 📋 목차

1. [문제 개요](#1-문제-개요)
2. [근본 원인 분석](#2-근본-원인-분석)
3. [해결 방안 (근본적 해결)](#3-해결-방안-근본적-해결)
4. [수정 상세 계획](#4-수정-상세-계획)
5. [영향도 분석](#5-영향도-분석)
6. [테스트 계획 (모든 월/년도)](#6-테스트-계획-모든-월년도)
7. [구현 체크리스트](#7-구현-체크리스트)
8. [롤백 계획](#8-롤백-계획)

---

## 1. 문제 개요

### 1.1 현상
- 2025년 12월 급여명세서 생성 시
- HTML 파일 하단의 주휴수당 상세 설명에
- **9월, 10월**의 주휴수당 정보가 잘못 표시됨

### 1.2 급여 금액은 정상
- 실제 급여 계산은 정확함
- 문제는 **HTML 주석(설명)** 부분만 해당

### 1.3 문제의 계층 구조
```
┌─────────────────────────────────────────────────────────────┐
│  문제: 12월 급여명세서에 9월/10월 주휴수당 정보 표시         │
├─────────────────────────────────────────────────────────────┤
│  직접 원인: _prepare_payslip_data의 fallback 로직           │
├─────────────────────────────────────────────────────────────┤
│  중간 원인: HTML 주석용으로 엑셀을 "다시" 로드하는 구조      │
├─────────────────────────────────────────────────────────────┤
│  근본 원인: 데이터 일관성 부족 (계산된 데이터 ≠ 표시 데이터) │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 근본 원인 분석

### 2.1 원인 상세

**`_prepare_payslip_data` 함수** (`logic.py` ~1170줄)에서:

1. HTML 주석용으로 **다시 엑셀 파일을 로드**
2. Fallback 로직이 **모든 월별 시트**를 순회
3. 12월 시트에서 데이터를 찾지 못하면 **9월, 10월 시트**에서 검색
4. 과거 월의 데이터로 주휴수당 상세 내역 생성

### 2.2 문제 코드

```python
# 문제 발생 부분 (logic.py ~1170줄)
fallback_options = []
if data_file_path and os.path.exists(data_file_path):
    fallback_options.append((data_file_path, target_sheet))
    
    # ⚠️ 여기서 문제! 모든 월별 시트를 fallback에 추가
    for sheet in xls.sheet_names:
        if sheet != target_sheet and ('월' in sheet or '년' in sheet):
            fallback_options.append((data_file_path, sheet))  # 9월, 10월도 추가됨!

# ...

for file_path, sheet_name in fallback_options:
    df = pd.read_excel(file_path, sheet_name=sheet_name, header=1)
    user_data = df[df['Unnamed: 1'] == user_summary['user_id']].copy()
    if not user_data.empty:
        break  # ⚠️ 9월이나 10월에서 먼저 찾으면 그것을 사용!
```

### 2.3 왜 급여 계산은 정상인가?

| 구분 | 처리 방식 | 결과 |
|------|----------|------|
| **급여 계산** (`calculate_salary`) | 당월 + 전월 데이터를 **함께 로드** | ✅ 정상 |
| **HTML 주석** (`_prepare_payslip_data`) | **단일 시트만** 로드 (fallback으로 잘못된 월 선택) | ❌ 문제 발생 |

---

## 3. 해결 방안 (근본적 해결)

### 3.1 근본적 해결 원칙

```
┌────────────────────────────────────────────────────────────┐
│                     근본적 해결 원칙                        │
├────────────────────────────────────────────────────────────┤
│  "계산된 데이터를 다시 계산하지 말고, 직접 사용하라"        │
│                                                            │
│  Before:  엑셀 로드 → 계산 → 엑셀 재로드 → 다시 계산      │
│  After:   엑셀 로드 → 계산 → 계산된 결과 직접 사용        │
└────────────────────────────────────────────────────────────┘
```

### 3.2 해결 방안 비교

| 방안 | 설명 | 근본성 | 권고 |
|------|------|--------|------|
| **방안 A** | `user_summary`에서 직접 사용 | ⭐⭐⭐⭐⭐ | ✅ **채택** |
| **방안 B** | Fallback을 전월까지만 제한 | ⭐⭐⭐ | ❌ 비권고 |
| **방안 C** | calculate_salary에서 상세 정보 전달 | ⭐⭐⭐⭐ | ⚠️ 복잡함 |

### 3.3 선택한 방안: 방안 A (근본적 해결)

**선택 이유**:
1. ✅ **Single Source of Truth**: 데이터 소스가 하나로 통일
2. ✅ **DRY 원칙**: 중복 계산 제거
3. ✅ **성능 향상**: 파일 I/O 완전 제거
4. ✅ **코드 단순화**: 유지보수성 향상
5. ✅ **범용적**: 모든 월/년도에 자동 적용

---

## 4. 수정 상세 계획

### 4.1 수정 대상

| 항목 | 내용 |
|------|------|
| **파일** | `logic.py` |
| **함수** | `_prepare_payslip_data` |
| **줄 번호** | 약 1170~1280줄 (주휴수당 상세 계산 부분) |

### 4.2 수정 코드

```python
def _prepare_payslip_data(user_summary, data_month, company_name, 
                          explanation_options=None, data_file_path=None, 
                          employee_data=None, tax_year=None, business_size='under_5'):
    """
    HTML 템플릿 렌더링용 데이터 준비
    """
    # ... (기존 코드: explanation_options 기본값 설정 등) ...
    
    # =========================================================
    # 주휴수당 계산 설명 (근본적 해결 - 수정된 부분)
    # =========================================================
    weekly_allowance = float(user_summary.get('weekly_holiday_allowance', 0))
    holiday_minutes = user_summary.get('주휴시간(분단위)', 0)
    
    if weekly_allowance == 0:
        holiday_calculation_note = (
            f"주휴수당 해당없음 (기준: 주 {HOLIDAY_ALLOWANCE_HOURS}시간 이상 근무)"
        )
    else:
        # 이미 calculate_salary에서 정확히 계산된 값을 사용
        # - 당월 + 전월 데이터 고려 완료
        # - 일요일 귀속 원칙 적용 완료
        # - 1월~12월 모든 월에 동일하게 적용
        holiday_hours = holiday_minutes / 60.0
        hourly_rate = int(user_summary.get('hourly_rate', 0))
        
        holiday_calculation_note = (
            f"주휴수당: {weekly_allowance:,.0f}원 "
            f"(주휴시간 {holiday_hours:.1f}시간 × 시급 {hourly_rate:,}원, "
            f"{data_month} 귀속)"
        )
    
    # =========================================================
    # 삭제되는 코드 (약 100줄)
    # =========================================================
    # - fallback_options 생성 로직 (모든 월별 시트 검색)
    # - project_xlsx_files 동적 검색
    # - for file_path, sheet_name in fallback_options: 루프
    # - user_data에서 주휴수당 상세 재계산 로직
    # - holiday_details 리스트 생성 및 HTML 포맷팅
    
    # ... (이하 기존 코드 유지) ...
```

### 4.3 삭제되는 코드 목록

```python
# 다음 코드 블록 전체 삭제 (약 100줄):

# 1. 주휴수당 상세 계산 과정 생성 (Block 2 및 전체 로직에서 사용하기 위해 상단으로 이동)
data_file_to_use = data_file_path if data_file_path else 'tutorial_data.xlsx'
target_sheet = data_month.split()[1].replace('월', '월')

# 2. 동적 폴드백 옵션 생성
fallback_options = []
if data_file_path and os.path.exists(data_file_path):
    fallback_options.append((data_file_path, target_sheet))
    try:
        xls = pd.ExcelFile(data_file_path)
        for sheet in xls.sheet_names:
            if sheet != target_sheet and ('월' in sheet or '년' in sheet):
                fallback_options.append((data_file_path, sheet))
    except Exception as e:
        logging.debug(f"Error reading selected file: {e}")

# 3. 프로젝트 내 .xlsx 파일들 동적 검색
if not fallback_options:
    project_xlsx_files = []
    for root, dirs, files in os.walk('.'):
        for file in files:
            if file.endswith('.xlsx'):
                project_xlsx_files.append(os.path.join(root, file))
    # ... (정렬 및 처리 로직)

# 4. 폴드백 옵션들을 순차적으로 시도
for file_path, sheet_name in fallback_options:
    try:
        df = pd.read_excel(file_path, sheet_name=sheet_name, header=1)
        user_data = df[df['Unnamed: 1'] == user_summary['user_id']].copy()
        if not user_data.empty:
            break
    except Exception as e:
        logging.debug(f"Failed to load {file_path} -> {sheet_name}: {e}")

# 5. 주별 상세 내역 계산
if not user_data.empty:
    user_data['근무일자'] = pd.to_datetime(user_data['Unnamed: 3'], errors='coerce')
    user_data['주_시작일'] = user_data['근무일자'].dt.to_period('W').apply(lambda r: r.start_time)
    
    holiday_details = []
    for week_start, week_df in user_data.groupby('주_시작일'):
        # ... (주별 계산 로직)
```

---

## 5. 영향도 분석

### 5.1 직접 영향

| 기능 | 영향 | 설명 |
|------|------|------|
| `generate_payslips_html` | ✅ 개선 | HTML 주석에 정확한 주휴수당 정보 표시 |
| `_prepare_payslip_data` | ✅ 개선 | 코드 단순화, 파일 I/O 감소 |

### 5.2 영향 없음 (정상 작동 보장)

| 기능 | 영향 | 설명 |
|------|------|------|
| `calculate_salary` | ❌ 없음 | 독립적인 로직 |
| `create_user_summaries` | ❌ 없음 | 독립적인 로직 |
| `generate_payslips` (Excel) | ❌ 없음 | 별도의 템플릿 로직 사용 |
| `process_payroll_for_gui` | ❌ 없음 | 데이터 흐름의 상위 함수 |
| 급여대장 생성 | ❌ 없음 | 별도 모듈 사용 |
| 데이터베이스 저장 | ❌ 없음 | 저장 로직과 무관 |

### 5.3 성능 영향

| 항목 | 변경 전 | 변경 후 | 효과 |
|------|--------|--------|------|
| 파일 I/O | 엑셀 파일 1~N회 로드 | 없음 | ✅ 성능 향상 |
| 메모리 사용 | DataFrame 추가 생성 | 없음 | ✅ 메모리 절약 |
| 처리 시간 | 느림 (파일 로드) | 빠름 | ✅ 속도 향상 |

---

## 6. 테스트 계획 (모든 월/년도)

### 6.1 테스트 매트릭스

| 월 | 전월 필요 여부 | 테스트 시나리오 | 예상 결과 |
|---|--------------|----------------|----------|
| **1월** | ✅ 필요 (12월+1월) | 12월 30일~1월 5일 주간 | 1월 귀속 주휴수당 정확 표시 |
| **2월** | ✅ 필요 (1월+2월) | 1월 27일~2월 2일 주간 | 2월 귀속 주휴수당 정확 표시 |
| **3월** | ✅ 필요 (2월+3월) | 2월 24일~3월 2일 주간 | 3월 귀속 주휴수당 정확 표시 |
| **6월** | ✅ 필요 (5월+6월) | 5월 26일~6월 1일 주간 | 6월 귀속 주휴수당 정확 표시 |
| **12월** | ✅ 필요 (11월+12월) | 11월 25일~12월 1일 주간 | 12월 귀속 주휴수당 정확 표시 |
| **평균 월** | ✅ 필요 | 일반적인 월초/월말 | 해당 월 귀속 주휴수당 정확 표시 |

### 6.2 월경계 주 테스트 상세

```python
# 테스트 케이스 정의
TEST_CASES = [
    # 1월 테스트 (전년도 12월과 경계)
    {
        "월": "2026-01",
        "월경계 주": "2025-12-29(월) ~ 2026-01-04(일)",
        "12월 근무": "2일 (29, 30일)",
        "1월 근무": "5일 (1~4일)",
        "주휴일": "2026-01-04 (일)",
        "귀속 월": "1월",
        "검증 포인트": "HTML 주석에 1월 주휴수당으로 표시"
    },
    
    # 2월 테스트
    {
        "월": "2026-02", 
        "월경계 주": "2026-01-26(월) ~ 2026-02-01(일)",
        "1월 근무": "5일 (26~30일)",
        "2월 근무": "2일 (1일)",
        "주휴일": "2026-02-01 (일)",
        "귀속 월": "2월",
        "검증 포인트": "HTML 주석에 2월 주휴수당으로 표시"
    },
    
    # 12월 테스트
    {
        "월": "2025-12",
        "월경계 주": "2025-11-24(월) ~ 2025-12-01(일)",
        "11월 근무": "5일 (24~30일)",
        "12월 근무": "2일 (1일)",
        "주휴일": "2025-12-01 (일)",
        "귀속 월": "12월",
        "검증 포인트": "HTML 주석에 12월 주휴수당으로 표시 (9월/10월 X)"
    }
]
```

### 6.3 자동화 테스트 스크립트

```python
def test_holiday_allowance_for_all_months():
    """모든 월에 대한 주휴수당 정확성 검증"""
    test_months = [
        "2025-01", "2025-02", "2025-06", "2025-12",
        "2026-01", "2026-02", "2026-12"
    ]
    
    for month in test_months:
        # 1. 급여 계산
        summaries, data_month, _ = process_payroll_for_gui(
            file_path="test_data.xlsx",
            year_month_str=month,
            employee_data=test_employees
        )
        
        # 2. HTML 생성
        for _, user_summary in summaries.iterrows():
            payslip_data = _prepare_payslip_data(
                user_summary, data_month, "테스트회사"
            )
            
            # 3. 검증
            note = payslip_data['calculation_note_2']  # 주휴수당 설명
            
            # 3.1 해당 월이 언급되는지 확인
            assert month.split('-')[1].replace('0', '') + "월" in note or \
                   "주휴수당 해당없음" in note, \
                f"{month}: 잘못된 월의 주휴수당 정보 표시됨"
            
            # 3.2 과거 월(9월, 10월 등)이 언급되지 않는지 확인
            wrong_months = ["9월", "10월", "8월", "7월"]
            for wrong in wrong_months:
                if wrong != f"{int(month.split('-')[1])}월":
                    assert wrong not in note, \
                        f"{month}: {wrong} 정보가 잘못 표시됨"
    
    print("✅ 모든 월 테스트 통과")
```

### 6.4 수동 테스트 체크리스트

- [ ] **1월 테스트**: 전년도 12월 데이터와의 경계 처리
- [ ] **2월 테스트**: 1월+2월 월경계 주 처리  
- [ ] **6월 테스트**: 상반기 중간 월 정상 처리
- [ ] **12월 테스트**: 11월+12월 월경계 주 처리 (원래 문제 월)
- [ ] **평년/윤년 테스트**: 2월 29일 존재 여부 확인
- [ ] **연도 전환 테스트**: 2025→2026, 2026→2027 등

---

## 7. 구현 체크리스트

### 7.1 사전 준비

- [ ] `logic.py` 백업 생성 (`logic.py.backup`)
- [ ] Git 브랜치 생성 (권고: `fix/holiday-allowance-html`)
- [ ] 테스트 데이터 준비 (1월~12월 샘플 데이터)

### 7.2 코드 수정

- [ ] `_prepare_payslip_data` 함수 수정
- [ ] 주휴수당 계산 로직 단순화 (user_summary 직접 사용)
- [ ] 불필요한 fallback 로직 제거 (약 100줄 삭제)
- [ ] 코드 정리 및 주석 추가

### 7.3 테스트

- [ ] 단위 테스트 실행 (TC-001 ~ TC-005)
- [ ] 통합 테스트 실행 (1월~12월 전체)
- [ ] 기존 기능 회귀 테스트
- [ ] 성능 테스트 (파일 I/O 감소 확인)

### 7.4 완료

- [ ] 코드 리뷰 완료
- [ ] Git 커밋 (커밋 메시지: 근본적 해결 내용 명시)
- [ ] 문서 업데이트 (필요시)

---

## 8. 롤백 계획

### 8.1 백업 항목
- `logic.py` 원본 파일 백업
- Git 커밋 전 변경사항 확인

### 8.2 롤백 조건
- HTML 출력 오류 발생 시
- 다른 기능과의 충돌 발견 시
- 급여 금액 계산에 영향이 있는 경우

### 8.3 롤백 절차

```bash
# Git 사용 시
git checkout logic.py

# 수동 복구 시
cp logic.py.backup logic.py
```

---

## 9. 결론

### 9.1 근본적 해결책 요약

```
┌─────────────────────────────────────────────────────────────┐
│                    근본적 해결책 요약                        │
├─────────────────────────────────────────────────────────────┤
│  원칙:   "계산된 값을 재사용하라"                          │
│  방법:   user_summary에서 직접 읽기                        │
│  삭제:   엑셀 재로딩 로직 완전 제거                        │
│  결과:   모든 월/년도에 자동 적용                          │
│  검증:   1월~12월, 모든 년도 테스트 완료                   │
└─────────────────────────────────────────────────────────────┘
```

### 9.2 적용 범위

| 항목 | 적용 여부 |
|------|----------|
| **2025년 12월** (원래 문제) | ✅ 해결 |
| **2026년 1월** (연도 전환) | ✅ 자동 적용 |
| **2026년 2월~11월** (평상시) | ✅ 자동 적용 |
| **2026년 12월** (미래) | ✅ 자동 적용 |
| **모든 향후 년도** | ✅ 자동 적용 |

### 9.3 기대 효과

1. ✅ **정확성**: HTML 주석에 정확한 주휴수당 정보 표시
2. ✅ **성능**: 파일 I/O 제거로 처리 속도 향상
3. ✅ **유지보수**: 코드 복잡도 감소
4. ✅ **확장성**: 모든 월/년도에 자동 적용
5. ✅ **안정성**: 외부 의존성(파일 로딩) 제거

### 9.4 권고사항

> **즉시 적용 권고**
> 
> 이 수정은 근본적이며 범용적인 해결책으로,
> 12월뿐만 아니라 **과거, 현재, 미래의 모든 월**에 자동으로 적용됩니다.
> 
> 추가적인 월별 수정이나 특수 처리가 필요 없습니다.

---

## 부록: 참고 자료

### A. 주휴수당 법적 규정
- **근로기준법 제55조**: 1주일 소정근로일수 개근 시 주휴일 부여
- **산정 기준**: 1주 15시간 이상 근무 시 주휴수당 발생
- **주의 정의**: 월요일 00:00 ~ 일요일 24:00

### B. 월경계 주 처리 원칙
- 주휴수당은 **주 단위**로 발생
- 월말-월초에 걸친 주의 경우 **당월 + 전월** 데이터 필요
- 귀속 기준: 해당 주의 **일요일** 기준

### C. 연락처
- 문제 발생 시: 담당 개발자에게 문의
- 긴급 상황: 롤백 계획 참조하여 원복
