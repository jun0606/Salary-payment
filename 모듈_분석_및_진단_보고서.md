# 🔍 모듈 분석 및 진단 보고서

> **분석일**: 2026년 2월 9일  
> **분석 대상**: main_qt.py 실행 시 발생하는 ModuleNotFoundError

---

## 1. 오류 현황

### ❌ 발생한 오류

```
ModuleNotFoundError: No module named 'cryptography'
```

**오류 발생 위치**:
- `license_system/license_verifier.py`, line 10
- `tax_settings_qt.py`에서 라이선스 시스템 import 시

---

## 2. 상세 분석

### 🔍 문제 1: cryptography 모듈

**현재 상태**:
```bash
# 이미 설치됨 (requirements.txt 기준)
cryptography==43.0.1
```

**원인 분석**:
- Python 버전 충돌 가능성 (Python 3.14)
- 가상환경 문제
- 설치된 위치와 실행되는 Python 경로 불일치

**확인 필요**:
```bash
which python3
python3 -c "import cryptography; print(cryptography.__version__)"
```

---

### 🔍 문제 2: winreg 모듈 (Windows 전용)

**오류 로그**:
```
다크 모드 감지 오류: No module named 'winreg'
```

**원인**:
- `winreg`는 Windows 전용 모듈
- macOS에서는 사용 불가
- 다크 모드 감지 코드에 플랫폼 체크가 없음

**해결 방안**:
```python
# 다크 모드 감지 코드 수정 필요
import sys
import platform

if platform.system() == 'Windows':
    import winreg
    # Windows 다크 모드 감지
else:
    # macOS/Linux 대체 로직
    pass
```

---

### 🔍 문제 3: 라이선스 시스템 구조 문제

**오류 로그**:
```
'DummyLicenseVerifier' object has no attribute 'LicenseManager'
```

**원인**:
- `DummyLicenseVerifier` 클래스에 `LicenseManager` 속성이 없음
- 라이선스 마이그레이션 코드의 버그

---

## 3. Python 환경 진단

### 현재 Python 경로
```bash
/opt/homebrew/Cellar/python@3.14/3.14.2/...
```

### 설치된 패키지 확인
```bash
pip3 list | grep -E "(cryptography|PyQt6|pandas|numpy)"
```

---

## 4. 해결 방안

### 단기 해결책 (즉시 적용 가능)

#### 1. Python 경로 통일
```bash
# 현재 사용 중인 Python 확인
which python3
python3 --version

# pip3와 python3 경로 일치 확인
ls -la $(which python3)
ls -la $(which pip3)
```

#### 2. 모듈 재설치
```bash
pip3 uninstall cryptography -y
pip3 install cryptography==43.0.1 --break-system-packages
```

#### 3. PYTHONPATH 확인
```bash
echo $PYTHONPATH
python3 -c "import sys; print('\n'.join(sys.path))"
```

### 중기 해결책 (코드 수정 필요)

#### 1. winreg 호출 부분 수정
- `main_qt.py`의 다크 모드 감지 코드에 플랫폼 체크 추가

#### 2. 라이선스 시스템 수정
- `DummyLicenseVerifier` 클래스에 `LicenseManager` 속성 추가

---

## 5. 권장 조치사항

### 즉시 실행 (ACT 모드)

1. **Python 환경 통일 확인**
2. **cryptography 재설치**
3. **프로그램 재실행 테스트**

### 추후 개선

1. **크로스 플랫폼 호환성 개선** (winreg 제거)
2. **라이선스 시스템 구조 개선**

---

## 6. 실행 명령어

```bash
# 1. Python 경로 확인
which python3

# 2. cryptography 모듈 테스트
python3 -c "from cryptography.hazmat.primitives import hashes; print('OK')"

# 3. 전체 모듈 테스트
python3 -c "
import PyQt6
import pandas
import openpyxl
import jinja2
import holidays
import cryptography
print('All modules OK')
"
```

---

## 7. 결론

**주요 문제**: Python 3.14 + macOS 환경에서의 모듈 경로 문제

**해결 우선순위**:
1. 🔴 Python 경로 통일 및 cryptography 재설치
2. 🟡 winreg 호출 부분 플랫폼 체크 추가
3. 🟢 라이선스 시스템 구조 개선
