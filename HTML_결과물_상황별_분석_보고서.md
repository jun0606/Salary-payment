# HTML 결과물 상황별 분석 보고서

## 1. 5인 미만 vs 5인 이상 사업장 비교

### 1.1 연장수당 표시

| 사업장 규모 | 계산 배수 | HTML 표시 문구 |
|------------|-----------|----------------|
| **5인 미만** | 1.0배 | "1.5배 (5인미만 가산의무 없음)" |
| **5인 이상** | 1.5배 | "1.5배" |

**구현 위치**: `logic.py` - `_prepare_payslip_data()` 함수

```python
premium_label = "배"
if business_size == 'under_5' and ot_multiplier == 1.5:
    premium_label = "배 (5인미만 가산의무 없음)"
```

---

### 1.2 야간수당 표시

| 사업장 규모 | 계산 배수 | HTML 표시 문구 |
|------------|-----------|----------------|
| **5인 미만** | 1.0배 | "X시간 * 시급 Y원 (5인미만)" |
| **5인 이상** | 1.5배 | "X시간 * 시급 Y원 * 1.5배" |

**구현 위치**: `logic.py` - `_prepare_payslip_data()` 함수

```python
if business_size == 'under_5':
    night_multiplier = 1.0
    display_detail = f"{night_hours:.1f}시간 * {hourly_rate:,}원 (5인미만)"
else:
    night_multiplier = 1.5
    display_detail = f"{night_hours:.1f}시간 * {hourly_rate:,}원 * 1.5배"
```

---

### 1.3 휴일근무 표시

| 사업장 규모 | 8시간 이내 | 8시간 초과 |
|------------|-----------|-----------|
| **5인 미만** | 1.0배 | 1.0배 |
| **5인 이상** | 1.5배 | 2.0배 |

**구현 위치**: `logic.py` - `calculate_salary()` 함수

---

### 1.4 근무시간 한도 체크

| 사업장 규모 | 주 52시간 초과 체크 | 로깅 |
|------------|-------------------|------|
| **5인 미만** | ❌ 없음 | 60시간 초과 시 정보 로깅 |
| **5인 이상** | ✅ 있음 | 52시간 초과 시 경고 로깅 |

---

## 2. 4대보험 적용 여부 비교

### 2.1 보험료 계산

| 구분 | 4대보험 대상자 | 4대보험 비대상자 |
|------|---------------|-----------------|
| **국민연금** | 총급여 * 4.5% | 0원 |
| **건강보험** | 총급여 * 3.545% | 0원 |
| **장기요양** | 건강보험 * 12.81% | 0원 |
| **고용보험** | 총급여 * 0.9% | 0원 |

**구현 위치**: `logic.py` - `create_user_summaries()` 함수

```python
insurance_eligible = employee.get('insurance_eligible', True)

if insurance_eligible:
    # 4대보험 정상 계산
else:
    # 4대보험 0원 처리
```

---

### 2.2 소득세 계산

| 구분 | 4대보험 대상자 | 4대보험 비대상자 |
|------|---------------|-----------------|
| **세율** | 누진세율 (6%~45%) | 단일세율 3% |
| **과세표준** | 총급여 - 4대보험 - 기본공제 | 총급여 |

---

## 3. HTML 출력 결과 비교

### 3.1 지급항목 테이블 (동일 구조)

```html
<tr>
    <td>기본급</td>
    <td class="amount">X원</td>
    <td>국민연금</td>
    <td class="amount">X원</td>
</tr>
<tr>
    <td>주휴수당</td>
    <td class="amount">X원</td>
    <td>건강보험</td>
    <td class="amount">X원</td>
</tr>
```

**차이점**: 값만 다르게 전달, HTML 구조는 동일

---

### 3.2 계산식 설명 영역

**5인 미만 사업장**:
```
연장근무 2.0시간 × 시급 10,000원 × 1.5배 (5인미만 가산의무 없음)
야간근무 1.5시간 × 시급 10,000원 (5인미만)
```

**5인 이상 사업장**:
```
연장근무 2.0시간 × 시급 10,000원 × 1.5배
야간근무 1.5시간 × 시급 10,000원 × 1.5배
```

---

## 4. 종합 평가

| 항목 | 5인 미만 | 5인 이상 | 4대보험 O | 4대보험 X |
|------|---------|---------|-----------|-----------|
| **연장수당** | 기본만 | 1.5배 | - | - |
| **야간수당** | 기본만 | 1.5배 | - | - |
| **휴일근무** | 기본만 | 1.5/2.0배 | - | - |
| **국민연금** | - | - | 정상계산 | 0원 |
| **건강보험** | - | - | 정상계산 | 0원 |
| **고용보험** | - | - | 정상계산 | 0원 |
| **장기요양** | - | - | 정상계산 | 0원 |
| **소득세** | - | - | 누진세율 | 3% |

---

## 5. 설정 방법

### 5.1 사업장 규모 설정
```json
// config.json
{
    "business_size": "under_5"  // 또는 "over_5"
}
```

### 5.2 4대보험 적용 여부 설정
```json
// employees.json
{
    "190335406": {
        "name": "홍유민",
        "insurance_eligible": true  // 또는 false
    }
}
```

---

## 6. 결론

✅ **HTML 출력은 동일한 구조를 사용**하며, 값만 상황에 맞게 계산되어 전달됨

✅ **5인 미만/이상 구분**이 연장/야간/휴일 수당 계산에 정확히 적용됨

✅ **4대보험 적용 여부**에 따라 보험료 및 세금 계산이 정확히 분기됨

✅ **사용자는 config.json과 employee_data 설정**으로 쉽게 제어 가능
