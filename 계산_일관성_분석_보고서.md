# 계산 일관성 분석 보고서

> **분석일**: 2026년 2월 9일  
> **대상 파일**: `logic.py`  
> **분석 목적**: 계산함의 일관성을 물어뜨리는 부분 식별 및 개선 방안 제시

---

## 1. 개요

본 보고서는 급여 계산 시스템의 일관성을 분석하여, 잠재적인 버그나 불일치 가능성이 있는 부분을 식별하고 개선 방안을 제시합니다.

---

## 2. 발견된 일관성 문제점

### 2.1 🟡 중간 위험: 수당(`allowances`) 처리의 일관성 문제

**위치**: `_prepare_payslip_data` 함수 (약 1100라인)

**문제점**:
```python
# 추가 수당 항목들 (직원별 수당 데이터)
allowances = {}  # 초기화 추가 (최근 수정됨)
if employee_data and str(user_summary['user_id']) in employee_data:
    employee = employee_data[str(user_summary['user_id'])]
    allowances = employee.get('allowances', {})
    
    # 지속적 수당 추가
    for allowance in allowances.get('recurring', []):
        ...

# 해당 월 단발성 수당 추가
for allowance in allowances.get('one_time', []):  # <-- 여기서 사용
```

**분석**:
- ✅ **최근 수정으로 초기화 추가됨**: `allowances = {}`
- ⚠️ **하지만 여전히 일관성 문제 존재**:
  - 지속적 수당(`recurring`)은 `if` 블록 안에서만 처리
  - 단발성 수당(`one_time`)은 `if` 블록 밖에서 처리
  - 단발성 수당 처리 로직이 `if` 조건과 무관하게 실행됨

**개선 제안**:
```python
# 통일된 처리 방식
allowances = {}  # 초기화
if employee_data and str(user_summary['user_id']) in employee_data:
    allowances = employee_data[str(user_summary['user_id'])].get('allowances', {})

# 지속적 수당 처리
for allowance in allowances.get('recurring', []):
    ...

# 단발성 수당 처리
for allowance in allowances.get('one_time', []):
    ...
```

---

### 2.2 🟡 중간 위험: 총 급여액 계산의 데이터 소스 불일치

**위치**: `calculate_salary` vs `_prepare_payslip_data`

**문제점**:
```python
# calculate_salary 함수
df['총급여액'] = df['예상급여금액'] + df['주휴수당'].fillna(0)

# _prepare_payslip_data 함수
total_payment = (user_summary.get('base_pay', 0) +
                user_summary.get('weekly_holiday_allowance', 0) +
                user_summary.get('연장수당', 0) +  # <-- 이름 불일치
                user_summary.get('night_pay', 0) +
                user_summary.get('수당합계', 0))
```

**분석**:
- `calculate_salary`에서는 `예상급여금액` + `주휴수당`으로 계산
- `_prepare_payslip_data`에서는 개별 항목을 더해서 계산
- 컬럼명 불일치: `연장수당` vs `overtime_pay` vs `extra_pay`

**위험성**:
- 데이터 소스가 다를 경우 금액 불일치 가능성
- 유지보수 시 수정 누락 가능성

**개선 제안**:
```python
# _prepare_payslip_data에서 일관되게 사용
total_payment = user_summary.get('총급여액', 0)  # calculate_salary에서 계산된 값 사용
```

---

### 2.3 🟢 낮은 위험: 금액 절사 방식의 일관성

**위치**: `create_user_summaries` 함수

**현재 구현**:
```python
def round_down_to_10_won(amount):
    """10원 단위로 절사 (10원 이하 절사)"""
    return int(amount / 10) * 10

# 사용처
national_pension = round_down_to_10_won(total_pay * national_pension_rate)
health_insurance = round_down_to_10_won(total_pay * health_insurance_rate)
...
```

**분석**:
- ✅ 모든 금액 계산에 동일한 절사 함수 사용
- ✅ 일관된 방식으로 처리됨

---

### 2.4 🟡 중간 위험: 5인 미만/이상 사업장 처리의 분산

**위치**: `calculate_salary` 함수 및 `_prepare_payslip_data` 함수

**문제점**:
```python
# calculate_salary 함수
business_size = "over_5"
if employee_data and str(user_id) in employee_data:
    ...
    business_size = companies[company_id].get('business_size', 'over_5')

if business_size == 'under_5':
    # 5인 미만 처리
else:
    # 5인 이상 처리

# _prepare_payslip_data 함수 (별도로 전달받음)
def _prepare_payslip_data(..., business_size='under_5'):
```

**분석**:
- `calculate_salary`에서 한 번 결정된 `business_size`가
- `_prepare_payslip_data`로 전달됨
- 하지만 두 함수에서 각각 다시 계산/확인하는 패턴 존재

**위험성**:
- `process_payroll_for_gui`에서 가져온 `business_size`와
- `calculate_salary` 낮에서 결정된 `business_size`가 다를 가능성

**개선 제안**:
```python
# process_payroll_for_gui에서 한 번 결정하고 일관되게 전달
business_size = config_data.get('business_size', 'under_5')
# 모든 하위 함수에 동일한 값 전달
```

---

### 2.5 🟡 중간 위험: 시간 단위 변환의 일관성

**위치**: 여러 함수에서 사용

**문제점**:
```python
# parse_work_hours 함수
return hours * 60 + minutes  # 분 단위 반환

# calculate_salary 함수
total_hours = total_minutes / 60.0  # 시간 단위로 변환

# _prepare_payslip_data 함수
work_hours = work_minutes / 60.0  # 시간 단위로 변환
```

**분석**:
- ✅ 변환 로직은 일관됨
- ⚠️ **하지만** `_prepare_payslip_data`에서 계산된 `holiday_hours`가
  `calculate_salary`에서 계산된 값과 다를 수 있음

---

### 2.6 🔴 높은 위험: `applied` 플래그의 상태 변경

**위치**: `_prepare_payslip_data` 함수

**문제점**:
```python
for allowance in allowances.get('one_time', []):
    if apply_date.startswith(target_date) and not applied:
        ...
        allowance['applied'] = True  # 상태 변경!
```

**분석**:
- 단발성 수당 처리 시 `applied` 플래그를 변경
- 이는 **원본 데이터를 수정**하는 부작용(side effect)
- 같은 데이터로 여러 번 계산할 경우 문제 발생 가능

**위험 시나리오**:
1. 1월 급여 계산 → 수당 적용 → `applied = True`
2. 동일 데이터로 재계산 시 → 수당 누락

**개선 제안**:
```python
# 복사본 사용 또는 플래그 제거
applied_flags = {}  # 별도 관리
```

---

### 2.7 🟢 낮은 위험: 세법 기준 로드의 중복

**위치**: `create_user_summaries`와 `_prepare_payslip_data`

**분석**:
```python
# create_user_summaries
tax_standards = tax_manager.get_standards_for_year(str(tax_year))

# _prepare_payslip_data
tax_standards = tax_manager.get_standards_for_year(str(tax_year))
```

- 각 함수에서 독립적으로 로드
- `TaxLawManager`가 캐싱을 하므로 성능 문제는 없음
- 일관성은 유지됨

---

## 3. 개선 우선순위 및 권고사항

### 즉시 개선 권고 (🔴)

| 우선순위 | 항목 | 개선 내용 |
|---------|------|----------|
| 1 | `applied` 플래그 수정 | 원본 데이터 수정 방지, 별도 상태 관리 |
| 2 | 총 급여액 계산 통일 | `calculate_salary`의 계산 결과를 신뢰하고 재계산하지 않기 |

### 중기 개선 권고 (🟡)

| 우선순위 | 항목 | 개선 내용 |
|---------|------|----------|
| 3 | 수당 처리 로직 통일 | `recurring`과 `one_time` 처리를 동일한 블록에서 |
| 4 | `business_size` 결정 중앙화 | 한 곳에서 결정하여 모든 함수에 전달 |
| 5 | 컬럼명 표준화 | `연장수당`/`overtime_pay`/`extra_pay` 통일 |

### 정상 유지 (🟢)

- ✅ 금액 절사 방식
- ✅ 세법 기준 로드 방식

---

## 4. 종합 평가

| 평가 항목 | 상태 | 설명 |
|----------|------|------|
| **데이터 소스 일관성** | 🟡 개선 필요 | 총 금액 재계산 패턴 존재 |
| **계산 로직 일관성** | 🟡 개선 필요 | 5인 미만/이상 처리 분산 |
| **출력 형식 일관성** | ✅ 양호 | Excel과 HTML 모두 동일 데이터 사용 |
| **상태 관리 일관성** | 🔴 심각 | `applied` 플래그 수정 문제 |

---

## 5. 결론

현재 시스템은 **기본적인 계산 일관성은 유지**되고 있으나, 다음과 같은 개선이 필요합니다:

1. **즉시**: `applied` 플래그 상태 변경 문제 해결
2. **단기**: 총 금액 계산의 중복 제거
3. **중기**: 수당 처리 및 사업장 규모 결정 로직 중앙화

주휴수당 문제는 이미 근본적으로 해결되었으며, 추가로 발견된 일관성 문제들은 상대적으로 낮은 위험도를 가집니다.
