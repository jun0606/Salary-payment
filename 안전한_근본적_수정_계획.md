# 🔒 안전하고 근본적인 수정 계획

> **작성일**: 2026년 2월 9일  
> **목표**: 계산 일관성 문제의 근본적 해결

---

## 1. 현재 상태

### ✅ 이미 해결된 문제
- 주휴수당 HTML 출력 문제 (근본적 해결)
- `allowances` 변수 초기화 버그

### ⚠️ 해결 필요한 문제
1. **`applied` 플래그 상태 변경** (🔴 높은 위험)
2. **총 급여액 계산 중복** (🟡 중간 위험)

---

## 2. 수정 계획

### 수정 1: `applied` 플래그 문제 해결 ⭐⭐⭐⭐⭐

**문제**: `_prepare_payslip_data`에서 원본 데이터 수정
```python
# 기존 (문제)
allowance['applied'] = True  # 원본 데이터 수정!
```

**해결**: 복사본 사용 또는 플래그 제거
```python
# 개선 (안전)
# 별도 상태 관리 또는 플래그 제거
```

**안전성**: ⭐⭐⭐⭐⭐ (원본 데이터 보호)
**영향도**: 높음 (재계산 시 데이터 무결성)

---

### 수정 2: 총 급여액 계산 통일 ⭐⭐⭐⭐⭐

**문제**: 두 곳에서 각각 계산
```python
# calculate_salary
df['총급여액'] = df['예상급여금액'] + df['주휴수당'].fillna(0)

# _prepare_payslip_data (재계산)
total_payment = (user_summary.get('base_pay', 0) + ...)
```

**해결**: `calculate_salary` 결과 신뢰
```python
# 개선 (일관성)
total_payment = user_summary.get('총급여액', 0)
```

**안전성**: ⭐⭐⭐⭐⭐ (Single Source of Truth)
**영향도**: 중간 (데이터 일관성)

---

## 3. 수정 우선순위

| 순위 | 항목 | 안전성 | 영향도 | 소요시간 |
|-----|------|--------|--------|---------|
| 1 | `applied` 플래그 | ⭐⭐⭐⭐⭐ | 높음 | 30분 |
| 2 | 총 금여액 통일 | ⭐⭐⭐⭐⭐ | 중간 | 20분 |

---

## 4. 롤백 계획

```bash
# 문제 발생 시 복구
cp logic.py.backup.v2 logic.py
```

---

## 5. 검증 방법

```bash
# 테스트 실행
python3 test_real_data_december.py
python3 test_holiday_fix.py
```

모든 테스트 통과 시 수정 완료.
