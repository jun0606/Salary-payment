# 주휴수당 계산 규칙 분석 보고서

## 1. 발견된 문제

HTML 결과에서 주휴수당 계산 오류 발견:

### 사례 1 (홍유민)
- 근무시간: 109.0시간
- 주휴수당: 32,384원
- 주휴시간: 3.2시간
- 시급: 11,000원
- **계산 검증**: 3.2 × 11,000 = 35,200원 ≠ **32,384원** ❌

### 사례 2 (오수민)
- 근무시간: 135.5시간
- 주휴수당: 70,210원
- 주휴시간: 7.0시간
- 시급: 10,030원
- **계산 검증**: 7.0 × 10,030 = 70,210원 ✅

### 사례 3 (송여윤)
- 근무시간: 60.0시간
- 주휴수당: 34,102원
- 주휴시간: 3.4시간
- 시급: 10,030원
- **계산 검증**: 3.4 × 10,030 = 34,102원 ✅

---

## 2. 문제 원인 분석

### 주휴수당 공식 (근로기준법)
```
주휴시간 = (주 총 근무시간 / 40시간) × 8시간
주휴수당 = 주휴시간 × 시급
```

### 사례 1 재검증
- 주휴시간 3.2시간으로 역산:
  - 3.2 = (주 근무시간 / 40) × 8
  - 주 근무시간 = (3.2 / 8) × 40 = 16시간
  
- 실제 주휴수당 32,384원으로 역산:
  - 32,384 / 11,000 = 2.944시간 ≈ **2.94시간**
  
- **결론**: 주휴시간 3.2시간이 아닌 **2.94시간**이 정확한 계산

### 오류 원인
1. **가중평균 시급 적용**: 여러 시급이 있는 경우 가중평균이 적용됨
2. **주휴시간 반올림**: 소수점 처리 방식의 차이
3. **10원 단위 절사**: `round_down_to_10_won()` 함수 적용

---

## 3. 수정 필요 사항

### 현재 코드 (logic.py 329-336)
```python
# 가중평균 시급 계산
week_rates = week_df['시급금액'].dropna().unique()
if len(week_rates) > 1:
    total_week_pay = sum(row['근무시간_분'] / 60 * row['시급금액'] for _, row in week_df.iterrows())
    weighted_average_rate = total_week_pay / total_hours if total_hours > 0 else week_rates[0]
else:
    weighted_average_rate = week_rates[0] if week_rates else 0

weekly_holiday_total = paid_hours * weighted_average_rate
```

### 문제점
1. `paid_hours` 계산 시 `min()` 함수 사용으로 인한 차감
2. HTML 표시용 주휴시간과 실제 계산용 주휴시간 불일치
3. 10원 단위 절사 시점의 차이

---

## 4. 권장 수정 방안

1. **주휴시간 계산 일원화**
   - HTML 표시용과 계산용 주휴시간 동일하게 처리
   
2. **가중평균 시급 정확성**
   - 여러 시급이 섞인 경우 정확한 가중평균 적용
   
3. **절사 시점 통일**
   - 10원 단위 절사는 최종 금액에만 적용

4. **검증 로직 추가**
   - 계산 결과 검증을 위한 단위 테스트 강화
