# PyQt6 기반 급여명세서 생성기 의존성
# 배포용 requirements.txt - 버전 고정으로 안정성 확보

# =============================================================================
# 필수 의존성 (배포에 반드시 포함)
# =============================================================================

# GUI 프레임워크
PyQt6==6.7.1
PyQt6-Qt6==6.7.2

# 데이터 처리 (배포용 고정 버전)
pandas==2.2.2
openpyxl==3.1.5
numpy==1.26.4

# 템플릿 엔진
jinja2==3.1.4

# 암호화 및 보안 (라이선스 시스템용)
cryptography==43.0.1

# 날짜/공휴일 처리
holidays==0.55

# =============================================================================
# 플랫폼별 의존성 (Windows 전용)
# =============================================================================

# 시스템 정보 (Windows 하드웨어 ID용)
wmi==1.5.1; sys_platform == "win32"

# =============================================================================
# 배포 도구 (개발/배포 시에만 설치)
# =============================================================================

# PyInstaller (배포용 exe 생성)
# pyinstaller==6.10.0

# 배포 자동화 도구 (선택사항)
# auto-py-to-exe==2.44.1

# =============================================================================
# 설치 방법
# =============================================================================
#
# 1. 기본 설치 (사용자용):
#    pip install -r requirements.txt
#
# 2. 개발환경 설치 (배포 도구 포함):
#    pip install -r requirements.txt
#    pip install pyinstaller auto-py-to-exe
#
# 3. PyInstaller로 exe 생성:
#    pyinstaller --onefile --windowed --name=급여명세서관리 main_qt.py
#
# =============================================================================
# 참고사항
# =============================================================================
#
# - Python 3.9+ 권장
# - Windows 10/11 지원
# - PyQt6은 GUI가 포함된 완전한 프레임워크
# - 모든 의존성은 PyPI에서 배포 가능
# - 라이선스 시스템은 cryptography로 암호화
