# 시트에 없는 인원이 HTML에 포함되는 문제 - 근본적 수정 계획

## 📋 문제 개요

**문제**: HTML 급여명세서 생성 시 선택된 시트에 없는 직원이 포함되는 로직 오류

**현재 상황**: 
- 시트에 없는 직원(예: 김나연)이 HTML 결과에 포함됨
- 시스템이 employees.json 전체를 기준으로 급여 데이터를 생성

## 🎯 해결 목표

시트에 실제로 존재하는 직원만 HTML 결과에 포함되도록 로직 수정

## 📝 수정 계획

### 1. 핵심 함수 추가

#### `filter_sheet_employees(df)`
```python
def filter_sheet_employees(df):
    """시트에 실제로 존재하는 직원 ID 목록을 추출"""
    sheet_employee_ids = set(df['Unnamed: 1'].dropna().unique())
    return sheet_employee_ids
```

### 2. 기존 함수 수정

#### `process_payroll_for_gui(file_path, year_month_str, employee_data=None)`
```python
def process_payroll_for_gui(file_path, year_month_str, employee_data=None):
    # ... 기존 로직 ...
    
    # 시트에 실제로 존재하는 직원 ID 추출
    sheet_employee_ids = filter_sheet_employees(df)
    
    # create_user_summaries 호출 시 시트 직원 ID 전달
    summaries = create_user_summaries(df_calculated, employee_data, tax_year=year, sheet_employee_ids=sheet_employee_ids)
    
    # ... 나머지 로직 ...
```

#### `create_user_summaries(df, employee_data=None, tax_year=None, sheet_employee_ids=None)`
```python
def create_user_summaries(df, employee_data=None, tax_year=None, sheet_employee_ids=None):
    # ... 기존 로직 ...
    
    # 시트에 없는 직원 제외 (핵심 수정)
    if sheet_employee_ids:
        user_summaries = user_summaries[user_summaries['user_id'].isin(sheet_employee_ids)]
    
    # ... 나머지 로직 ...
```

### 3. HTML 생성 함수 유지
- `generate_payslips_html` 함수는 자동으로 필터링된 데이터만 처리
- 추가 수정 필요 없음

## 🔧 구현 세부사항

### 1. 데이터 흐름
```
Excel 파일 → load_and_preprocess_data → calculate_salary → create_user_summaries → generate_payslips_html
     ↓              ↓                    ↓                  ↓                        ↓
   시트 데이터 → 시트 직원 ID 추출 → 급여 계산 → 시트 직원만 필터링 → HTML 생성
```

### 2. 핵심 검증 로직
- 시트에 없는 직원이 HTML 결과에 포함되지 않음
- 시트에 있는 직원만 정확히 포함됨
- 퇴사자 처리 로직은 기존과 동일하게 유지

### 3. 테스트 시나리오
- **시트에 없는 직원(김나연)이 HTML 결과에 포함되지 않는지 확인**
- **시트에 있는 직원들만 정확히 포함되는지 확인**
- **다른 시트로 변경 시 동일하게 작동하는지 확인**

## 📊 예상 효과

### 1. 정확성 향상
- 선택한 시트에 맞는 정확한 HTML 결과 제공
- 시트에 없는 직원이 포함되는 오류 완전히 방지

### 2. 사용자 경험 개선
- 시트 선택과 HTML 결과의 일관성 확보
- 불필요한 혼란 방지

### 3. 시스템 안정성
- 로직 일관성 확보
- 유지보수 용이성 향상

## ⚠️ 주의사항

### 1. 하위 호환성
- 기존 API 호출 방식은 그대로 유지
- 선택적 파라미터로 추가하여 하위 호환성 보장

### 2. 성능 고려
- 추가되는 필터링 로직은 경량화
- 대규모 데이터에도 영향 최소화

### 3. 에러 처리
- 시트 직원 ID 추출 실패 시 예외 처리
- 빈 시트에 대한 안전한 처리

## 🧪 검증 방법

### 1. 단위 테스트
```python
def test_sheet_employee_filtering():
    # 시트에 없는 직원이 HTML에 포함되지 않음 확인
    # 시트에 있는 직원만 정확히 포함됨 확인
```

### 2. 통합 테스트
```python
def test_html_generation_consistency():
    # 실제 시트 데이터와 HTML 결과 비교
    # 시트 변경 시 일관성 확인
```

### 3. 회귀 테스트
```python
def test_existing_functionality():
    # 기존 기능에 영향 없음 확인
    # 다른 시트에서도 정상 작동 확인
```

## 📅 구현 일정

### Phase 1: 핵심 함수 구현 (1일)
- `filter_sheet_employees` 함수 추가
- `create_user_summaries` 함수 수정

### Phase 2: 연동 로직 구현 (1일)
- `process_payroll_for_gui` 함수 수정
- 파라미터 전달 로직 구현

### Phase 3: 테스트 및 검증 (1일)
- 단위 테스트 작성
- 실제 데이터로 검증
- 에러 케이스 테스트

### Phase 4: 문서화 및 배포 (0.5일)
- 수정 사항 문서화
- 사용법 안내 작성

## 🎉 기대 결과

1. **문제 해결**: 시트에 없는 직원이 HTML에 포함되는 문제 완전 해결
2. **정확성 확보**: 시트와 HTML 결과의 완전한 일치
3. **사용자 만족**: 직관적이고 일관성 있는 시스템 제공
4. **유지보수성**: 명확한 로직 구조로 인한 유지보수 용이성

---

*이 수정 계획은 시스템의 근본적인 문제를 해결하기 위한 체계적인 접근 방식을 제시합니다.*